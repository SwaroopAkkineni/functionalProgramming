/***
    Pure Functions ~ functions with no side effects
        plus is a type of first class function
        uses the concept of Referential Transparency(RT)
    Referential Transparency(RT) ~ calling the same function with the same input results in the same return value
    Side Effects ~ doing somethign other than simply returns a variable
    decomposition/extraction ~ destructuring an object from the expression
        val (left, right) = Pair.destructure()
    first class function ~ functions that can be passes as first class citizens
        aka can be passed as variables

 */


fun main() {

}